var carros = ['gol', 'palio', 'sandero', 'fusca']

for(var carro of carros) { //nao mostra o indice do array, usar dependendo da situaçao (nao entendi bem)
    console.log(carro) //a cada interacao do laço é passado para variavel criada (carro) o que está no array carros, imprimindo cada elemento a cada novo laço
}

