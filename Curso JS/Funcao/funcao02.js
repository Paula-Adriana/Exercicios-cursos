function soma(n1,n2) {
    return n1+n2
}
console.log (soma (2,23))

/*
function soma(n1=0,n2=0) {
    return n1+n2
}
console.log (soma (2))
Se não passar os dois parametros para o programa ele retornará NaN (not a number) entao pode-se definir
os parametros anteriormente se for o caso. Por ex, n1=0 ou n2 = 0, assim se nenhum numero for definido de 
antemãoo JS considera o parametro já fornecido que é zero. 
*/ 